<player_animation>
    !ifexp !($(debug) || $(dev)) # Во время отладки отключаем все анимации, чтобы не ждать их загрузки при подключении
        is_block_on_jetpack=true
    
        !tryinclude player_animation_std.inc
    !endif
</player_animation>
